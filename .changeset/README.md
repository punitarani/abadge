# Changesets

Changeset files in this directory drive the version PR and GitHub Release flow for the abadge CLI.

Only `@abadge/cli` is release-managed right now. CLI release-surface changes should include a
changeset so the release workflow can open or update the version PR on `main`.

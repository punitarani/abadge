---
"@abadge/mcp": patch
---

Docs: revamp the public API reference around the REST `/v1` surface and remove tRPC from the documentation. The MCP documentation pages (`apps/docs/mcp/*`) had their tRPC procedure references replaced with the corresponding REST routes or plain-language descriptions. Documentation-only change; no MCP runtime behavior changes.

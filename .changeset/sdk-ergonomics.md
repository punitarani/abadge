---
"@abadge/cli": patch
"@abadge/mcp": patch
---

SDK ergonomics: immutable-update footguns now throw, and `AbadgeApiError` carries `requestId`. `packages/sdk` is bundled into the CLI and MCP release binaries.

---
"@abadge/cli": patch
"@abadge/mcp": patch
---

docs: reconcile reference docs with the current code and remove deprecated references. Corrects the MCP tool name (`use_secret`), the canonical `read`/`use` capability model (including the item-target vs profile-target grant distinction), keypair-only agent auth, and the CLI permission/run examples.

## Worker environment variable keys
## Derived from Bindings in apps/api/src/types.ts,
## excluding Cloudflare-managed bindings (e.g. HYPERDRIVE).
## Used by: generate-dev-vars.sh
## Note: generate-dev-vars.sh only writes keys that currently have non-empty values.

# Database (local dev / fallback when not using Hyperdrive binding)
DATABASE_URL

# Public URLs
ABADGE_API_URL
ABADGE_APP_URL

# Credential encryption (AES-256-GCM)
ENCRYPTION_KEY

# Auth — Better Auth
BETTER_AUTH_SECRET
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET

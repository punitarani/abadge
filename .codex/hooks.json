{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun run format && bun run lint:fix && bun run typecheck && bun run build",
            "timeout": 600,
            "statusMessage": "Formatting, linting, type checking, and building..."
          }
        ]
      }
    ]
  }
}

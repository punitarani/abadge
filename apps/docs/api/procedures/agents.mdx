---
title: "agents.*"
description: "Register, list, and revoke automated callers."
---

The `agents` router manages the automated callers that consume credentials.

## `agents.create`

**Tier:** `sessionProcedure` (scoped to the active organization — the agent is owned by the session's active org; `organizationId` is not a request field).

<ParamField path="name" type="string" required />
<ParamField path="kind" type="string" required>
  `local_cli`, `local_mcp`, or `remote`.
</ParamField>
<ParamField path="authMethod" type="string">
  `public_key_session` — the only value, and the default.
</ParamField>
<ParamField path="publicKey" type="string">
  Ed25519 JWK for direct enrollment of `public_key_session` agents. JSON-serialized JWK string.
</ParamField>
<ParamField path="issueBootstrapToken" type="boolean">
  Issue a one-time `abe_...` enrollment token instead of direct enrollment.
</ParamField>
<ParamField path="metadata" type="object">
  Free-form metadata stored on the agent record. Use this for `description`, owner tags, etc.
</ParamField>

**Returns:**

```ts
{
  agent: Agent;
  bootstrapToken: string | null;    // shown once
  bootstrapExpiresAt: string | null;
}
```

Behavior:

- Agents must provide either `publicKey` or `issueBootstrapToken: true`. Missing both yields `PUBLIC_KEY_REQUIRED`.
- Bootstrap tokens have a 10-minute TTL.

## `agents.list`

**Tier:** `sessionProcedure`

**Returns:** `{ agents }`.

## `agents.get`

**Tier:** `sessionProcedure`

<ParamField path="agentId" type="string" required />

**Returns:** `{ agent }`.

## `agents.revoke`

**Tier:** `sessionProcedure`

<ParamField path="agentId" type="string" required />

**Returns:** `{ ok: true }`.

Invalidates all active `abs_...` sessions and disables future authentication. Permissions are not deleted but become useless until the agent is re-enabled.

## `agents.self`

**Tier:** `agentProcedure`

Return the calling agent's own record. Useful for runtime self-introspection.

**Input:** none.

**Returns:** `{ agent }`.

## Audit events

- `agent.create`
- `agent.bootstrap_issue`
- `agent.enroll`
- `agent.revoke`
- `agent.revoke_cascade`
- `agent.session_issue`
- `agent.session_reject`
- `agent.session_revoke`
